先注册,在执行,
每次注册都会头插入到goroution中,执行从头开始

1.12在堆中分配defer的结构体和参数,然后将参数复制到栈中
1.13在栈中分配defer的结构体和参数,然后复制参数到栈,但是循环的defer还是要1.12的
1.14直接在最后面运行函数,不用生成结构体,设置标识位标记是否执行函数,在编译阶段插入代码,循环也要1.12
如果发生panic要,要重新扫描注册的defer函数 ,生成defer结构体链表
