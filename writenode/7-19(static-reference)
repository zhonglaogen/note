首次使用（static块）
执行main
new
class.forname
调用静态类成员变量，方法
继承

静态成员常量不会首次使用
子类调用父类静态子类不加载static块
成员块每次new都运行
钩子，拿到触发事件 Runtime类

引用：
强
软：不足gc'释放
弱：gc就会回收
虚：有个队列，一旦有gc的对象，队列就会得到这个值

正则表达式
模式匹配

arp欺骗。arp钓鱼

四次挥手：不丢数据
双方通过随机数确定身份
