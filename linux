cat 源文件 >> 追加的文件
cat 源文件 > 覆盖的文件

chmod rwx-rwx-rwx 文件名 当前用户 同组 其他用户
chown 用户名 文件名 更改文件或目录的所有者
chgrp 组名 文件名 更改文件所属的组
unmask 指定文件或目录创建时需要取消的权限，目录权限默认是777，文件权限默认是666
root的默认掩码为0022，普通用户权限默认为0022
第一位表示八进制数补位0
加上 -s 表示是字符串可以不加0
	file	dir
root 	644	755
normal	664	775	
