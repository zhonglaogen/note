redis 的整合需要自己写redismanager，放入序列化器


11异步处理
数据库邮件和短信，串性执行
数据库，短信和邮件新开线程，返回最长的时间
数据库消息队列，返回，异步邮件和短信
2应用解耦
订单和库存耦合
订单系统，消息队列，库存系统
3流量肖峰
秒杀进入消息队列，谁快谁进队列（限制人数），处理队列消息

docker run -d -p 5672:5672 -p 15672:15672 --name myrabbitmq 99cbda713eff
15672是网页管理器端口
用户名guest
密码guest


rabbitmq:
生产者
主机
虚拟机
交换机
queue

一条tcp多条通道
消费者

direct	1v1
fanout广播
topic
* (星号) 代表任意 一个单词
# (井号) 0个或者多个单词
