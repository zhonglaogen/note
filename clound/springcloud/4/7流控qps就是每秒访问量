qps是访问前进行限制，线程数是访问后进行限制

1默认直接，不符合配置就挂了
2关联，关联一个请求，如果A过了，关联的B也挂，例如支付挂了订单也就要挂
3链路：多个请求调用了同一个服务
psotman可以循环访问某个地址，线程数，延迟

1预热就是平时qps不多，突然就多了，这时候要让他慢慢上去
这时候最开始的容忍度就是 设置的值 / 负载因子（默认是3）   然后在接下来设定的时间内到达设置的值就可以了
可以有个缓冲的场景，例如秒杀
2排队等待：不拒绝，但是会一直等待前面完成
